# Data handling
pandas>=2.0           # Load and manipulate the CSV email dataset
numpy>=1.24           # Numerical arrays (feature vectors)

# Machine Learning 
scikit-learn>=1.3     # TF-IDF vectorizer, Logistic Regression, Random Forest,
                      # train_test_split, metrics (accuracy, F1, confusion matrix)
joblib>=1.3           # Save and load trained model files (.pkl)

# Natural Language Processing 
nltk>=3.8             # Tokenization, stop-word removal, lemmatization
spacy>=3.6            # Named entity recognition (sender names, orgs, URLs)

# Explainability 
lime>=0.2.0.1         # Local per-email explanations (highlights phishing words)
shap>=0.44            # Global feature importance (Shapley values)

# Attack Simulation 
requests>=2.31        # HTTP calls to OpenAI / Claude LLM APIs

# Frontend / Dashboard 
streamlit>=1.32       # Turns your Python script into an interactive web app
plotly>=5.18          # Interactive charts (confidence bar, ROC curve, pie chart)
matplotlib>=3.7       # Static charts (confusion matrix heatmap)